const navbarState = { //data tambahan untuk belajar combined reducer
    data: {
        company: 'imdadu company',
        logo: 'https://reqres.in/img/faces/12-image.jpg'
    }
};

const navbarReducer = (state = navbarState) => {
    return state;
};

export default navbarReducer;