const dataState = {
    data: {
        nama: 'imdadu',
        avatar: 'https://reqres.in/img/faces/12-image.jpg'
    }
};

const dataReducer = (state = dataState) => {
    return state;
};

export default dataReducer;